# Orvexa QA

Run from the Orvexa project root:

```text
npm run qa
```

The suite checks centralized release metadata, WinUI single-window wiring, catalog and local-state hardening, protocol limits and queueing, Setup configuration, web IDs and controls, responsive/mobile behavior, local assets, packs, trusted URLs and JavaScript syntax.
